pub struct UserChats {
    pub test_variable: u64,
}

impl UserChats {
    // Associated function to create a new Person instance
    pub fn new() -> Self {
        UserChats { test_variable: 55 }
    }
}
